repo: talkersoft-com/workflow-exec
tokens:
  WorkflowFolder: "{{DeckRoot}}/planning/workflow-exec"
  WorkflowPlanningFolder: "{{DeckRoot}}/planning/workflow-plans"
tasks:
  init:
    - "@status-check"
  jobs:
    - write-plans
    - handoff
  post: []
