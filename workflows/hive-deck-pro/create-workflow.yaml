repo: talkersoft-com/hive-deck-pro
tokens:
  WorkflowFolder: "{{DeckRoot}}/hive-deck-pro"
  WorkflowPlanningFolder: "{{DeckRoot}}/hive-deck-pro"
tasks:
  init:
    - "@status-check"
  jobs:
    - write-plans
    - handoff
  post: []
